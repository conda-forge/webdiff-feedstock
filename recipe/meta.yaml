{% set name = "webdiff" %}
{% set version = "1.4.0" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/webdiff-{{ version }}.tar.gz
  sha256: 90936fcfa80090f384e38aa6d5375c1865c7c47e2242c9e1435fe71eade4fe2e

build:
  entry_points:
    - webdiff = webdiff.app:run
    - git-webdiff = webdiff.gitwebdiff:run
    - git-webshow = webdiff.gitwebshow:run
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - poetry-core
    - pip
  run:
    - python >={{ python_min }}
    - binaryornot
    - pillow
    - pygithub 2.3.0
    - unidiff ==0.7.5  # https://github.com/danvk/webdiff/issues/236
    - git

test:
  imports:
    - webdiff
  commands:
    # - pip check  # https://github.com/danvk/webdiff/issues/236
    - webdiff --help
    - git-webdiff --help
    - git-webshow --help
  requires:
    - pip

about:
  home: https://github.com/danvk/webdiff/
  summary: Two-column web-based git difftool
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jonashaag
